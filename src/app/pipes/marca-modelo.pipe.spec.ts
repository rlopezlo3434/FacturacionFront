import { MarcaModeloPipe } from './marca-modelo.pipe';

describe('MarcaModeloPipe', () => {
  it('create an instance', () => {
    const pipe = new MarcaModeloPipe();
    expect(pipe).toBeTruthy();
  });
});
