import { FiltroProveedoresPipe } from './filtro-proveedores.pipe';

describe('FiltroProveedoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProveedoresPipe();
    expect(pipe).toBeTruthy();
  });
});
