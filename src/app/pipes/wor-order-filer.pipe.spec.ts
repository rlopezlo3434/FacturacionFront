import { WorOrderFilerPipe } from './wor-order-filer.pipe';

describe('WorOrderFilerPipe', () => {
  it('create an instance', () => {
    const pipe = new WorOrderFilerPipe();
    expect(pipe).toBeTruthy();
  });
});
